const pool =require("../../config/database");
const paths=require("../helper/constants/Paths");
const helper=require("../helper/helperfunctions");

module.exports={

    // getProfileData:(data,callback)=>{
    //     pool.query{
    //         `select * from `
    //     }
    // }
}
